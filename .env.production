# Production Environment Variables
# Use /api for proxy (configured in vite.config.ts or reverse proxy)
# Alternatively, use full backend URL if deployed separately
VITE_API_URL=/api

# Optional: Direct backend URL if not using proxy
# VITE_API_URL=https://your-backend-domain.com